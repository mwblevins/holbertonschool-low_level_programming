#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void);
{
	char lower;

	for (lower = 'a'; lower <= 'z'; lower++)
		_putchar(letter);

	_putchar('\n');

	return (0);
}
