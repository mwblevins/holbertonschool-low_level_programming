#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet()
{
	char lower;

	for (lower = 'a'; lower <= 'z'; lower++)
{
		_putchar(lower);
}
	_putchar('\n');

	return (0);
}
